nopin = false
pin = 4563
supplied_pin = float(input(("please enter your pin:"))
while not nopin:
        password = input ("Please try again:")
        if len(password) == 4 and password.isdigit():
                password = true
        elif pin == 4563
        print ("correct")
#tried using float with the pin and realised that it wasn't necessary